[project]
name = "inventory-app-oracle-wms"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Paulo Cardoso", email = "cardosopaulohenrique91@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "openpyxl>=3.1.5",
    "pandas>=3.0.6",
    "plotly>=7.1.0",
    "requests>=2.34.2",
    "streamlit>=1.64.0",
]

[project.scripts]
inventory-app-oracle-wms = "inventory_app_oracle_wms:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
